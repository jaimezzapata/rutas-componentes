import Header from "../helpers/Header";

const Servicios = () => {
  return (
    <div className="servicios">
      <Header />
      <section>
        <h1>Servicios</h1>
      </section>
    </div>
  );
};

export default Servicios;
